[package]
name = "jarvis-core"
version = "0.1.0"
description = "Security and policy boundary for the JARVIS Core gateway"
edition = "2021"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
