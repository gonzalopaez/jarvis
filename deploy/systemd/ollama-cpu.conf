[Service]
# Vulkan on the passed-through Navi 10 device measured substantially slower than CPU inference.
# Unset the experimental backend without modifying the vendor unit; rollback is removal of this drop-in.
UnsetEnvironment=OLLAMA_VULKAN GGML_VK_VISIBLE_DEVICES
